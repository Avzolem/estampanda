"use client";

// This component is currently not in use but kept for future reference
// It was part of the original template and uses next-auth which has been removed

const ButtonAccount = () => {
  // Component disabled - using simple auth instead of next-auth
  return null;
};

export default ButtonAccount;